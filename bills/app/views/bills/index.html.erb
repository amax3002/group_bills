<h1>Split your Expenses</h1>

<% link_to "Add an Expense", new_bill_path %>

<% @bills.each do |bill| %>
  <h4>
    <div>
      <%= link_to bill.name, bill.label_type %>
      <%= bill.amount %>
    </div>
  </h4>
<% end %>
