<h1>Split your Expenses</h1>

<%= link_to "Add an Expense", new_bill_path %>

<% @bills.each do |bill| %>
  <h4>
    <div>
      <%= link_to bill.amount, bill_path(bill.id) %>
      <%= bill.type %>
    </div>
  </h4>
<% end %>
